# 2026-08-18：上传资料问答优先级

## 任务目标

将用户明确的问答要求固化到 Skill：用户不需要查看原 PDF PNG、截图或 Word 视觉渲染；后续询问上传资料相关问题时，需要得到结论、资料/案例背景及结论理由。

## 实际完成内容

- 在 `SKILL.md` 增加 `18j`：上传资料知识问答先给结论，再说明实际来源背景、案例命中、理由、审查状态和适用边界。
- 扩展 `references/27_portable_108_evidence_pack.md`，新增 `User Knowledge-Answer Priority`：规定五段紧凑格式，并明确 PNG、截图、OCR JSONL 和 Word 是内部检索/核对辅助，不能成为用户获得答案的前置条件。
- 新增 T061 eval 与测试用例，禁止以视觉 QA 或图片查看替代用户要求的结论/理由；同时禁止把所有上传资料伪装成单条结论的直接证据。
- 更新滚动交接。

## 回答边界

- “不需要查看图片”不等于不保留或不使用原件。原 PDF/Word/XLSX 仍是证据原件；当精确截图、表格、公式、数字或低置信度 OCR 影响结论时，Agent 在内部回查原位置，并在文字回答中说明页码/位置和限制。
- 每次只列实际命中来源；若没有具体上传案例，必须写 `未命中具体案例`，不能编造案例背景。
- 案例、作者解释和审查结论继续分开；课程阈值和案例不能变成无条件广告动作。

## 验证与未执行项

| 检查 | 结果 |
| --- | --- |
| JSONL 测试用例可解析 | PASS：包含新增 T061 的全部测试行可解析 |
| Skill 结构校验 | PASS：`Skill is valid!` |
| `git diff --check` | PASS；仅 Git CRLF 预警 |

未执行账户广告决策、实时市场 MCP 调用、commit、push 或发布。

## 下一步入口

直接用真实问题验收 T061 格式，例如“某个广告 ACOS 高是否需要调整”；回答应先给条件化结论，再展示命中资料背景、相似案例及理由。视觉 Word QA 仅在用户要求核对排版时继续，不是知识问答阻塞项。
