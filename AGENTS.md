# Codex 项目协作规范

本文件是本仓库内所有 Codex 任务的强制入口。除非用户在当前任务中明确覆盖某条规则，否则必须遵守。

## 开始任务前

1. 读取本文件、`docs/PROJECT_REQUIREMENTS.md`、`docs/CODEX_HANDOFF.md`。
2. 涉及 Amazon 广告或关键词业务时，完整读取 `.agents/skills/amazon-ads-keyword-strategy/SKILL.md`，再按 References Map 读取相关参考文件。
3. 检查 `git status --short`，区分用户已有改动与本任务改动；不得覆盖或顺手整理无关文件。
4. 明确本轮目标、范围、禁止事项和验证方法。若用户规定阶段边界，不得提前执行下一阶段。
5. 优先复用 `data/processed/amazon_ads_skill/` 的现有产物；除非输入或产物损坏，不得无理由重跑上游语料管线。

## 执行规则

- 所有路径使用仓库相对路径；脚本不得写死本机绝对路径。
- 原始讲义和上传文件作为证据原件保存，不得静默改写。派生判断放入 processed 数据或参考文档。
- 案例、可执行规则、诊断假设、诊断问题、反例、评论信号和噪声必须分开。
- 不得把单篇讲义、单个案例、评论或未经验证的经验说法直接升级为通用规则。
- 涉及来源真实性、平台机制、政策或可能变化的功能时，优先核对 Amazon 官方一手资料，并记录核对日期。
- 不确定观点不得删除。必须保留不同观点、适用条件、反对证据、缺失数据和验证方法。
- 未经证据支持，不得用“错误”“已验证”“全部检查”等确定性措辞。
- 不建议违规操纵、虚假交易、侵权或规避平台政策的做法；相关内容只可作为风险证据保留并明确标记。

来源交叉验证必须遵守 `.agents/skills/amazon-ads-keyword-strategy/references/14_source_validation_and_conflict_protocol.md`。每批新资料至少要建立来源清单、重要主张清单和覆盖记录；只有覆盖记录证明已检查全部在范围内资料时，才可以声称“已与所有项目资料交叉验证”。

## 修改和验证

- 修改应保持最小且可追踪；业务规则变化要同步 `SKILL.md`、对应 reference、eval 或校验器中受影响的部分。
- 验证强度与改动风险匹配。至少执行 `git diff --check`、相关脚本或测试，以及 skill 校验器（适用时）。
- 验证失败只能报告 `FAIL` 或 `BLOCKED`，不得写成通过。未执行的检查必须标记 `NOT RUN`。
- 不自动 commit、push、发布、部署或删除文件，除非用户明确授权。

## 每次任务的交接闭环

每个实质性项目任务完成前必须：

1. 更新 `docs/CODEX_HANDOFF.md`，写明当前状态、最新交接、已知事实、未决问题和推荐下一步。
2. 在 `docs/handoffs/` 新增一份不可覆盖的任务记录，文件名使用 `YYYY-MM-DD-简短任务名.md`；同日重名时追加序号。
3. 记录任务目标、实际完成内容、修改文件及原因、新了解/决策、来源与置信度、验证命令和真实结果、风险/遗留事项、下一步入口。
4. 最终回复与交接文档一致，明确区分已完成、未执行和被阻塞内容。

若用户明确要求只读或禁止修改文件，则不得为了交接而破坏该边界；改为在最终回复中给出同等内容，并说明交接文档因只读约束未更新。
